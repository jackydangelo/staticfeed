<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
<channel>
    <title>{{ page_title }}</title>
    <link>{{ url }}</link>
    <description>RSS feed</description>
    <lastBuildDate>{{ last_build_date }}</lastBuildDate>
    <language>it</language>

    {% for a in articles %}
    <item>
        <title>{{ a.title }}</title>
        <link>{{ a.link }}</link>
        <guid isPermaLink="true">{{ a.link }}</guid>
        <description>{{ article.rss_description }}</description>
        <pubDate>{{ a.published }}</pubDate>
    </item>
    {% endfor %}

</channel>
</rss>
